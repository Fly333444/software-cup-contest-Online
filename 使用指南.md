# 训练指南 — PicoDet-S 火情检测模型

本指南说明如何从头训练、导出并打包提交文件。

---

## 1. 环境配置

创建 conda 环境并安装依赖：

```bash
conda create -n fire_env python=3.9 -y
conda activate fire_env

# 安装 PaddlePaddle GPU 版（CUDA 11.8）
pip install paddlepaddle-gpu==2.6.2 -f https://www.paddlepaddle.org.cn/whl/windows/mkl/avx/stable.html

# 安装 PaddleDetection
git clone https://gitee.com/paddlepaddle/PaddleDetection.git
cd PaddleDetection
git checkout release/2.8.1
pip install -r requirements.txt
python setup.py install
cd ..
```

> 如果用 CPU 训练，把上面的 `paddlepaddle-gpu` 改成 `paddlepaddle`。

---

## 2. 准备数据集

将标注好的 LabelMe JSON 文件放在 `A_train/` 目录下，结构如下：

```
submission/
├── A_train/
│   ├── Image/          ← 所有图片（.jpg）
│   ├── label/          ← LabelMe 标注文件（.json）
│   └── *.json          ← LabelMe 标注文件
```

运行转换脚本：

```bash
python convert_labelme_to_coco.py
```

这会生成 `A_train/train.json` 和 `A_train/val.json`（80%/20% 划分）。

> **注意**：部分图片不含任何目标（负样本），属于正常情况。训练配置已开启 `allow_empty: true`，负样本会参与训练以降低误检率。

---

## 3. 训练

```bash
python train.py
```

训练配置在 `configs/picodet_s_fire_100e.yml`，关键参数：

| 参数 | 值 |
|------|-----|
| 模型 | PicoDet-S (LCNet 0.75x) |
| 类别数 | 3 (battery, board, fire) |
| 输入尺寸 | 640×640 |
| 批次大小 | 4 |
| 学习率 | 0.02 (CosineDecay + LinearWarmup) |
| 训练轮数 | 100 |
| 预训练权重 | PPLCNet_x0_75 (自动下载) |

训练过程会在 `output/` 下保存检查点，每 10 轮保存一次。`best_model.pdparams` 是验证集 mAP 最高的模型。

### 保存训练日志

PaddleDetection 默认不写日志文件，建议用重定向：

```bash
python train.py 2>&1 | tee output/train.log
```

---

## 4. 评估模型

```bash
python evaluate.py
```

输出 COCO mAP 和每类 F1 分数。

### 训练结果（PicoDet-S, 100 epochs）

**总体指标：**

| 指标 | 值 |
|------|-----|
| mAP (IoU=0.50:0.95) | 0.655 |
| AP50 | 0.850 |
| AP75 | 0.808 |
| Mean F1 | **0.9546** |
| 推理速度 | 28.9 FPS (V100 达标) |
| 模型大小 | ~5 MB |

**每类指标 @ IoU=0.50：**

| 类别 | Precision | Recall | F1 |
|------|-----------|--------|-----|
| battery | 0.9091 | 1.0000 | 0.9524 |
| board | 1.0000 | 0.8500 | 0.9189 |
| fire | 1.0000 | 0.9848 | 0.9924 |

---

## 5. 导出模型（Paddle Inference 格式）

```bash
python export_model.py
```

导出后的文件在 `model/` 目录下：

```
model/
├── infer_cfg.yml       ← 预处理 & NMS 配置
├── model.pdmodel       ← 模型结构（~432 KB）
└── model.pdiparams     ← 模型权重（~4.7 MB）
```

> **注意**：如果导出时遇到中文路径问题，Paddle Inference 的 C++ 后端无法处理中文路径。解决方法是把项目放到全英文路径下，或者把 `model/` 复制到英文路径（如 `C:/temp_model/`）再测试。

---

## 6. 测试推理

```bash
# 创建图片列表文件
echo "A_train/Image/test.jpg" > data.txt

# 运行推理
python predict.py data.txt result.json

# 查看结果
cat result.json
```

输出格式（比赛要求）：

```json
{
  "result": [
    {
      "image_id": "test",
      "type": 1,
      "x": 100.0,
      "y": 200.0,
      "width": 50.0,
      "height": 80.0,
      "segmentation": []
    }
  ]
}
```

type 映射：1=battery, 2=board, 3=fire

---

## 7. 打包提交

```bash
python package.py
```

生成 `submission_v2.zip`，包含：

```
submission_v2.zip
├── predict.py
├── model/
│   ├── infer_cfg.yml
│   ├── model.pdmodel
│   └── model.pdiparams
└── PaddleDetection/deploy/python/
    ├── preprocess.py
    ├── utils.py
    └── keypoint_preprocess.py
```

上传到 AI Studio 即可。

---

## 项目文件说明

| 文件/目录 | 用途 |
|-----------|------|
| `train.py` | 训练入口，加载配置并启动 PicoDet-S 训练 |
| `evaluate.py` | 评估脚本，加载 best_model 在验证集上计算 mAP 和每类 F1 |
| `predict.py` | 推理脚本，比赛平台调用的入口（`python predict.py data.txt result.json`） |
| `export_model.py` | 将训练好的 checkpoint 导出为 Paddle Inference 格式（`model/`） |
| `convert_labelme_to_coco.py` | 将 LabelMe 标注转为 COCO 格式，并划分 train/val（80%/20%） |
| `package.py` | 打包脚本，检查必要文件并生成 `submission_v2.zip` |
| `configs/picodet_s_fire_100e.yml` | 主训练配置（模型结构、超参、数据增强等） |
| `configs/fire_detection_dataset.yml` | 数据集路径配置（被 yml 内联覆盖，保留供参考） |
| `configs/ppyoloe_s_fire_150e.yml` | PP-YOLOE-S 备选配置（未使用） |
| `model/` | 导出后的推理模型文件 |
| `output/` | 训练检查点（checkpoint），含 `best_model.pdparams` |
| `bbox.json` | 评估时生成的检测结果文件（COCO 格式），由 `evaluate.py` 读取计算 F1 |
| `A_train/` | 训练数据（图片 + LabelMe 标注 + 生成的 COCO JSON） |
| `PaddleDetection/` | PaddleDetection 框架源码 |
| `submission_v2.zip` | 打包好的提交文件，上传到 AI Studio |
| `TRAINING_GUIDE.md` | 本文档 |
| `提交说明.md` | 提交格式和评分规则说明 |

---

## 常见问题

| 问题 | 解决 |
|------|------|
| cudnn64_8.dll not found | 把 `conda环境/Library/bin/*.dll` 复制到 `conda环境/Lib/site-packages/paddle/libs/` |
| 代理连接失败 (ProxyError) | 关闭代理或取消 `HTTP_PROXY`/`HTTPS_PROXY` 环境变量后重试 |
| 训练时 loss 爆炸 | 降低学习率，检查数据集标注是否正确 |
| 导出后缺少 `.pdiparams` | `PaddleDetection/ppdet/engine/trainer.py` 第 1316 行的 `paddle.jit.save` 需要加 `combine_params=True` |
| 中文路径推理失败 | Paddle C++ 推理引擎不支持中文路径，复制 model 到英文路径测试 |
| 93 samples invalid | 正常现象，部分图片不含目标（负样本），配置已开启 `allow_empty: true` |
